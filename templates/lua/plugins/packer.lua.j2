-- Ansible managed

return require('packer').startup(function(use)
  use 'wbthomason/packer.nvim'

  --> regular plugins start
  {% for item in packer_plugins %}
    {{ item.use }}
  {% endfor %}
  --> regular plugins end

  {% if use_lsp %}
    --> LSP plugins start
    {% for item in lsp_packer_plugins %}
      {{ item }}
    {% endfor %}
    --> LSP plugins end
  {% endif %}

  {% if use_treesitter %}
    --> TreeSitter plugins start
    {% for item in treesitter_packer_plugins %}
      {{ item }}
    {% endfor %}
    --> TreeSitter plugins end
  {% endif %}

  -- Automatically set up your configuration after cloning packer.nvim
  -- Put this at the end after all plugins
  if packer_bootstrap then
    require('packer').sync()
  end
end)
