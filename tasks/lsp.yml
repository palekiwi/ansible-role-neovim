---
- name: Add lsp plugins to plugins.
  ansible.builtin.set_fact:
    plugins: "{{ plugins + lsp_plugins }}"

- name: Ensure required packages are installed.
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  become: true
  loop: "{{ lsp_system_packages }}"
